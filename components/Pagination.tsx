import {
    Paginator,
    Previous,
    Next,
    PageGroup,
    Container
} from 'chakra-paginator';

const Pagination = () => {
    return (
        <>
            <Paginator></Paginator>
        </>
    );
};

export default Pagination;
