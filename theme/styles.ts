import { mode } from '@chakra-ui/theme-tools';

const styles = {
    global: {
        body: {
            minH: '100vh',
            position: 'relative'
        }
    },
    a: {
        _hover: {
            textDecoration: 'none'
        }
    }
};

export default styles;
